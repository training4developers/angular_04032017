import keyMirror from 'key-mirror';

export const actionTypes = keyMirror({
    REFRESH_COLORS_REQUEST: null,
    REFRESH_COLORS_DONE: null,
    ADD_COLOR_REQUEST: null,
    ADD_COLOR_DONE: null,
    ADD_CAR: null,
});