'use strict';

import './app';

describe('tests', () => {

    it('some test pass', () => {
        expect(true).toBe(true);
    });

    it('some test fail', () => {
        expect(true).toBe(false);
    });

});